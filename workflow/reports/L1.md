# Assignment handoff: L1

- Domain / branch / worktree: Language / `rebuild-language` /
  `/Users/jon-mac-mini/code-stuff/github/freemocap/other/skellyspeak-language`.
- Base SHA: `51ec700048d7747069cb8ab364cb521bf4047091`; candidate uncommitted.
- State: **deterministic core ready for integration review**. Boundary requirements
  accepted; source implementation and normal Cargo verification complete. Provider,
  storage and UI assistance wiring are not implemented.
- Authorization: user approved scoped implementation after the design discussion.
  Integration accepted Unicode 17 extended boundaries and temporarily extended L1
  ownership to Cargo.toml/Cargo.lock for the pinned dependency and lib.rs for module
  registration only. No other shared file was edited. Git remained read-only.

## Concrete outcome and exact changes

Existing tracked files each gained exactly one line:

| File | Exact change |
| --- | --- |
| `src-tauri/Cargo.toml` | `unicode-segmentation = "=1.13.3"` under dependencies |
| `src-tauri/Cargo.lock` | `"unicode-segmentation",` in the application package dependency list; Cargo updated it offline, no package version/checksum change |
| `src-tauri/src/lib.rs` | `pub mod linguistics;` after languages |

New owned files: `src-tauri/src/linguistics/mod.rs`, `tests.rs`, `CONTRACT.md`,
`RECOMMENDATION.md`, `fixtures/README.md`, `fixtures/GraphemeBreakTest-17.0.0.txt`,
`fixtures/UNICODE-LICENSE.txt`, and this report. No generated TypeScript edits.

The core preserves exact immutable source identity and text; derives scalar, byte
and UTF-16 coordinates; bounds candidate size; rejects malformed, out-of-range,
overlapping, unordered and intra-grapheme spans; fills source gaps explicitly; and
reports structural complete/partial coverage separately from actual gloss counts.
It contains no provider, scheduler, credentials, persistence, retry or UI code.

New grapheme validation uses `grapheme_indices(text, true)` from the exact pinned
crate with Unicode 17 tables. Policy ID is `uax29-egc-17.0.0-us1.13.3-v1`. Every
candidate endpoint is checked. The ID catalog omits unsafe scalar positions rather
than renumbering or changing text. Unknown/interior IDs fail lookup. Byte/UTF-16/slice
utilities remain low-level coordinate conversions; callers must use candidate or
explicit grapheme validation before treating spans as interactive targets.

Individual word targets are preserved: `Unit::Phrase` is rejected in this word-gloss
layer with `PhraseRequiresSeparateLayer`. Phrase explanation needs a separate result
layer/contract; it cannot overwrite or erase individual targets. This is not removal
of phrase parity scope. Rewrote CONTRACT.md to remove obsolete phrase-substitution
and unwired/grapheme-unsafe descriptions. RECOMMENDATION.md retains its research/pilot
plan with a prominent note that the dependency/policy has since been implemented.

## Verification actually run

All Cargo commands used the worktree-local target, never a sibling worktree target:

```sh
export CARGO_TARGET_DIR=/Users/jon-mac-mini/code-stuff/github/freemocap/other/skellyspeak-language/src-tauri/target
cargo test --offline --manifest-path src-tauri/Cargo.toml --lib linguistics::
cargo clippy --offline --locked --manifest-path src-tauri/Cargo.toml --lib --tests -- -D warnings
cargo test --offline --locked --manifest-path src-tauri/Cargo.toml --lib
CARGO_NET_OFFLINE=true npm run contracts:check
rustfmt --edition 2024 --check src-tauri/src/linguistics/mod.rs src-tauri/src/linguistics/tests.rs
cargo fmt --manifest-path src-tauri/Cargo.toml -- --check
git diff --check
```

Results on Rust 1.98.1:

| Check | Result |
| --- | --- |
| Focused language Cargo tests | **21 passed**, 0 failed; 79 unrelated tests filtered |
| Full native library suite | **100 passed**, 0 failed; includes loopback adapter tests |
| Clippy, native lib/tests, warnings denied | Exit 0 |
| Generated contract check | Exit 0; no file regeneration |
| Owned Rust formatting | Exit 0 |
| Repository Cargo formatting | **Exit 1**: existing formatting differences only in unchanged `execution.rs` |
| Tracked diff whitespace | Exit 0 |

The formatting failure includes existing translation implementation/tests and predates
L1. `git diff` confirms the only modified tracked files are the three listed above.
Do not format R1's reserved execution file in this contribution. No claim that every
README check passed. Frontend tests/build/styles were not run: no frontend files or
generated contracts changed. Native GUI QA was not performed or implied.

### Unicode and application test evidence

The conformance test parses all **766** cases in the official Unicode 17
GraphemeBreakTest corpus, independently collects expected boundary markers, compares
the complete native catalog, tests expected allowed/rejected endpoints, and verifies
UTF-16 round-trips. It asserts corpus count and crate Unicode version. Corpus and
license are vendored with exact source URLs, SHA-256 checksums and copyright retained
in fixtures/README.md; tests need no network.

Application tests reject internal endpoints for combining accents, ZWJ/skin-tone
emoji, flags, variation selectors, keycaps, Indic conjuncts, Hangul Jamo and CRLF,
including gloss/literal/unresolved variants and ID lookup at both start and end.
Accepted whole clusters reconstruct source exactly. Other tests cover repeated words,
non-space scripts, whitespace, missing help, literal-only no-help results, size limits,
identity mismatch, invalid ranges/order/overlap, and ID/numeric equivalence. The prior
prototype test permitting an internal combining-mark boundary was replaced by actual
rejection tests. Earlier 19-test standalone results are superseded by these Cargo runs.

## Accepted contract requirements and remaining work

Integration's DATA-MODEL.md accepts immutable source identity, exact text, default
Unicode 17 extended boundaries, native UTF-16 conversion, independent word targets,
coverage distinct from available help, terminal valid partial results without automatic
retry, and failed retries retaining prior accepted help. Only deterministic structural
parts live in this core; accepted retry/publication semantics require execution/storage.

Still proposed: strict provider JSON shape, boundary-ID versus offset representation,
serialization of boundary policy/provenance, final persisted result model and additional
phrase/deep-insight/source-owner contracts. Strict JSON decoding is not implemented;
current Rust input values are typed. Language registry membership, partner ownership,
source validity and cancellation must be checked transactionally by integration.

The bounded evaluation recommendation lists 24 synthetic stimuli × two representations
× two repetitions = 96 planned Standard calls. At documented baseline pricing and
8,192 input / 2,048 total output token caps the bound is $0.7274496, with a proposed
$1 inference ceiling contingent on actual endpoint limits and repricing. The pilot
and harness remain **unexecuted**; Fast eligibility is not established.

U1 parity requirements remain: tap/drag word reveal, whole-bubble display, sentence
translation, selected-word analysis, deep contextual detail, learner analysis and
reading in suggestions/coach. Drag is not phrase analysis. Intentional deep inspection
can request help directly; mount/reopen/ordinary reveal do not request new inference.
Suggestions insert into the existing draft without sending; coach content stays private.
R2's written detail proposal uses a captured native word occurrence and a separate
result that cannot overwrite gloss targets. Full parity is not claimed by this core.

## Handoff and next integration step

- Native app launches: 0. Paid provider calls/attempts: 0. Unicode fixture downloads
  were read-only public-data retrieval, not inference. No Git mutations.
- Integration should review this exact uncommitted contribution and test results;
  user checkpoints/integrates via Git. Module registration allows normal cargo tests
  but does not declare a gloss operation or expose a new UI feature.
- Next shared work: reviewed serialized result/provenance policy and strict decoder,
  then existing planner/admission/publication wiring and generated frontend contracts.
  Add source deletion/cancellation/retry/reopen request-count tests for that wiring.
- No additional shared-file changes beyond the three authorized lines were made.
  Source/durable results remain in the assigned language worktree. The initial draft
  once written in the original task cwd was relocated before implementation; no
  original tracked source was changed.

## Next slice proposed separately from accepted core

Integration independently reran the 21 focused tests, found no blocking deterministic
core issue and accepted it for checkpoint/integration. Accepted source is now stable.
Added ADAPTER-PROPOSAL.md only: strict direct-from-text serde decoder, minimal tagged
boundary-ID spans (gloss or literal), omission-as-unresolved semantics, pure escaped
whole-passage prompt construction, finite byte/item bounds and offline fixture tests.
AI selects linguistic grouping; no whitespace/punctuation word tokenizer is proposed.
The proposal identifies existing prose-only request/publication seams and preserves
operation ownership. No adapter implementation, shared-file edits, extra tests or
provider calls in this proposal pass; earlier measured core results are unchanged.
