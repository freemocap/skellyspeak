# Wave 2 — B handback

Implemented native source, generated contracts/catalog, and fixture verification. No provider calls, application launch, data reset, or Git writes.

## Runtime behavior

- Store owns the validated editable YAML registry, initialized under workspace ownership before opening SQLite. Missing/invalid config produces actionable `config_load` startup refusal. Saved explanation language and initial English default must exist. Fresh schema is 14; prior versions including 13 are explicitly refused, without migrations.
- Accepted turns freeze resolved language context, full configuration hash, full construct registry hash, complete candidate constructs/count, feedback policy, focus/opportunity and prompt versions. Mandatory function/interaction/focus/prerequisite inclusion can exceed the soft 15–25 target. No due estimator was added.
- Actual partner, coach, translation, gloss and persona generation paths consume captured loaded guidance. Pragmatics is consumed by partner/opening/coach/persona generation. Generic legacy language wrappers remain test/standalone bundled fixtures.
- Candidate-specific observation schemas accept five outcomes and bounded exact-source evidence. Error observations require target hypothesis plus hint, elicitation and metalinguistic cue in the explanation language. Deterministic policy selects one correction; developmental/slip evidence is not automatically corrected. Safe snapshot/evidence summaries omit hidden hypotheses and raw rationale.
- Durable `open_card` records the exposed move before presentation. `show_answer` records explicit support. `keep_going` does not block chat. Revision captures exact prior item, selected rung and separately nullable actual `supportStep`. Pencil revisions before disclosure neither claim assistance nor advance the ladder or assistance depth. New advice resets exposure. Explicit is terminal, including unsuccessful subsequent revisions.
- Checked retries validate replaced-turn identity, exact quote and target evidence. Uncertain/absent evidence produces an explicit unconfirmed result, no fabricated error or Fixed claim. Successful repair produces a factual Fixed sentence using validated explanation-language rationale and a separate native `ix.self_repair` event with actual exposure support. Native repair replaces duplicate model self-repair judgment, remains visible read-only, and is excluded from current XP fold; target keeps existing two assisted XP. Wave 3 owns richer learner-model estimation.
- Learner/starter/surprise/described starts are snapshot-concurrent, receipt-idempotent and reject archived, stale, pending or already-started conversations. Partner openings have real operation graphs and assistant rows; no fabricated learner message, learner assessment or XP. Opening enters subsequent partner history/source IDs. Descriptions remain topic data/expression help. Suggestions operate without a missing user row. Late cancelled opening publication is rejected.
- Registry-incompatible evidence remains inspectable with an explicit mapping error and no current credit. Exact full construct hash gates credit even when visual catalog bytes are unchanged.
- Contract export generates and checks `src/contracts.ts` and `src/assets/skill-catalogs/catalog.json` from native/YAML source.

## Verification

- Complete native library suite: **284 passed**, including actual opening dispatch/publication/history and cancellation, explicit→failed retry, uncertainty, exposed graduated retry, KeepGoing, unseen independent retry, native repair uniqueness/no doubled credit, edited config prompts and same-catalog semantic hash mismatch.
- Final native Clippy `--lib -- -D warnings`: passed.
- Generated contracts/catalog `export-contracts --check`: passed.
- Whole native formatting applied.
- Added saved explanation-language refusal regression after the complete gate; parent runs final combined gate.

## Limits and follow-through

Literal cue leak validation handles short Latin targets on word boundaries; it cannot prove semantic non-disclosure or linguistic accuracy. Multilingual cue quality and real provider schema conformance require separate authorized live QA. Raw hypotheses/rationales stay durable privately for provenance, never indiscriminately disclosed.

No Bayesian/EWMA learner fold, review scheduler, or new proficiency claims. Workspace export remains database plus SQLite sidecars only; editable config is a separate local directory and factory reset clears it too. Root/C own final UX wording and runtime inspection.
