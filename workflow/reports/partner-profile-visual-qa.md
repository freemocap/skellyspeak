# Partner/lens profile visual check — 2026-09-13

Production LearnerModel was rendered in the existing ignored browser fixture at
`.local/learner-model-qa/`, using the already-running Vite server. Partner-scoped
responses and exclusion persistence were synthetic mocked IPC. Native app access
remained pending after the previously established Mac-lock limitation; no unlock,
restart, new server or native export was attempted here.

Inspected desktop light: Partner/Variety controls, registry-provided Interaction
and Pragmatics row groups, language-wide XP, source quote in Spanish/Arabic,
exclude/restore actions. Selecting a long-name partner fetched the scoped fixture;
source evidence stayed inspectable after exclusion. A complete exclude/restore
cycle restored the construct row and Exclude action.

Inspected dark at 360×800 with a long partner name. Page scroll width 360px; dialog
client and scroll widths both 307px. Controls wrap; the numeric table scrolls
horizontally inside the dialog. Archived partner with no evidence showed the
explicit empty state; switching to All partners restored rows. View/Save were
initially split by wrapping, so their buttons now share a wrapping action group.
No numeric lens aggregation was introduced. Viewport override was restored.

Screenshots were reviewed inline through browser tools; no screenshot files were
saved. These are rendered production-component checks with synthetic IPC, not
native storage verification. Automated scoped ownership/export/exclusion tests
are recorded separately by the integrator.
