# Latest U1 handoff — durable frontend diagnostics

Added nonrecursive console/caught/uncaught capture, safe cause/command IDs, explicit redacted argument counts and acknowledgement handling. Build and 379 frontend tests pass; combined gate has a launcher-test runner issue owned by Integration. [Exact coverage and limits](U1-durable-diagnostics.md).

# Latest U1 correction — model defaults only

Removed rejected Custom Voice input checkbox. Models remain populated/editable; blank edits restore known defaults on blur before autosave. Audio & Voice unchanged. 8 focused tests/build pass; source frozen. [Corrected handoff](U1-custom-voice-defaults.md).

# Latest U1 handoff — Custom voice defaults

Added Voice input toggle and known fresh Custom model defaults. Explicit off remains persistent; route/credentials unchanged. Full 374 tests and build pass. [Exact scope and repair ownership](U1-custom-voice-defaults.md).

# Latest U1 handoff — voice-first playback

Wired native speech playback/replay and stop/cancel lifecycle; enabled read-aloud and autosend controls using root's preference bridge. Full suite 370 tests and build pass. Source frozen for native microphone/output QA. [Exact implementation and limits](U1-voice-playback.md).

# Latest U1 handoff — AI access tabs

Restored the explicitly approved joined tabs with integrated route radio indicators; fixed checkbox overflow and compact credential controls. Build/style checks and 359 tests pass. [Source, reuse and browser verification](U1-access-tabs.md). No Git writes.

# Latest U1 handoff — dismissible errors

Added local dismissal to all compact error bubbles; new errors reappear. Composer typing/Enter/Send regression checks pass. Full suite: 358 tests; build/styles pass. [Exact changes and verification](U1-dismissible-errors.md). No Git writes.

# Latest U1 handoff — structural cleanup beneath reference UI

Extracted message-entry controls and saved-gloss status/retry presentation from large parent components. Existing markup/styles preserved; build and 354 tests pass. Source frozen for integration QA. [Exact scope and verification](U1-structure.md).

# Latest U1 handoff — compact reference presentation

Removed persistent missing-feature prose and repeated progress text; errors remain accessible through compact expandable details. Existing reference shell/controls and request behavior retained. 354 frontend tests, build and style check pass. [Detailed handoff and native QA readiness](U1-compact-presentation.md). No Git writes.

# Latest U1 handoff — coach sizing repair

The literal main-UI transplant remains the baseline. Completed the integration-authorized coach dialog/dock sizing repair in root; no redesign or production contract changes. Full frontend suite (353 tests), build and style check pass. Desktop/narrow/short static browser geometry checks pass; native QA remains integration-owned. See [coach sizing results and limits](U1-coach-sizing.md). No Git writes.

# Current U1 handoff — CSS cleanup

CSS ownership cleanup is complete. The strict checker, frontend build and all 342 frontend tests pass. Browser computed-style/geometry comparisons found no unexpected differences across the documented matrix. See [CSS results and limits](U1-css-results.md). Only styles.css and two graph presentation variable assignments changed in this round. No Git writes or second native app.

The prior UI-transplant handoff below describes the earlier completed contribution; its original stylesheet and verification statements are superseded by the CSS results above.

# U1 — Literal old UI and rebuild connections

## Active direction and ownership

The user authorized copying main's UI as-is and connecting the rebuild underneath. Main source eaa94bb509a12d83944e6f62a4514a5788112c5e was copied into rebuild-interaction (base 51ec700048d7747069cb8ab364cb521bf4047091). No Git writes. Integration owns shared generated contracts, Rust, lib/tauri.ts, GuidedPage and conversation controller. U1 owns main.tsx, App.tsx, SettingsModal/settings access and useMicRecorder. All domain tasks were informed of the change in direction.

## Implemented source

- Copied main frontend source, public assets and frontend manifests/config. Removed displaced frontend and obsolete docs/release-only tests; no alternate UI or reference Rust backend. Root stylesheet remains byte-identical to main. Three actively consumed catalog definition JSON files copied unchanged to frontend assets with two import-path updates; no learner data/XP migration.
- Startup awaits integration's actual native language loading, recognizes ai window label, and renders visible startup failure with explicit Retry. Browser playback lifecycle retained; old native close interception removed from startup because its automatic destroy behavior exceeded existing permission. No permissions expanded.
- App uses integration's getSettings/saveSettings projection and reloads authoritative revisions after saves. Removed old startup diagnostic/updater/gate/trace/evidence mount requests. Existing profile button reports evidence not connected when absent. AI panel native mapping remains integration work.
- Settings retains original navigation, sections and row classes. SettingsAccess uses get_connection/select_route/save_connection/disconnect, get_access_settings/save_access_settings/check_access, verify_openrouter_key and hosted commands. Credentials are write-only replacement inputs, status flags only for stored keys; no masks or secret readback. Deletion requires a separate confirmation. Saving credentials does not select the route. Custom means self-hosted SkellySpeak server with destination-bound token, not arbitrary OpenAI endpoint; no cloud-key fallback.
- Settings preference saves refresh authoritative scopes. Pending/failed writes keep modal open and retain input; retry is explicit. Unsupported persistent controls are disabled, including audio/TTS/autosend/reward/shortcuts/update/reset. No obsolete mounted mic capability/key validation requests. Access capability checks are user actions.
- Microphone hook now accepts conversationId and uses actual native recording IDs with mic_start/mic_wave/mic_cancel/mic_transcribe. No browser fallback or old aliases. Only explicit Stop transcribes. Cancel, unmount and conversation changes suppress late transcript callbacks and cancel active capture. Consumes the generated RecordingStarted response added by integration; original waveform uses its actual native samplesPerSecond and drains a bounded sample buffer once. No sample rate fabricated.

## Verification

- Transplant npm ci passed, zero reported vulnerabilities. Initial build passed. Initial retained frontend suite: 70 files/342 tests passed before controller rewiring.
- Current scoped settings tests: 11 passed. Cover configuration-only mount, no custom cloud-key fields, failed replacement retention/explicit retry, confirmed revision-scoped removal, supported reading preferences, disabled unsupported controls and pending-write close protection.
- Current microphone tests: 7 passed. Cover no mount work, conversation-owned start, explicit transcription, conversation-switch cancellation, late-start cleanup, exclusive transcription and late-result suppression, truthful capture failures and actual-rate/drain-once waveform behavior.
- Latest combined npm run build passed (304 modules). Final scoped settings/mic combined run: 18 passed. Full combined suite and native launch remain coordinator-owned after concurrent controller changes settle. Isolated successes are not native QA.
- Rebuild style checker fails on unchanged main CSS (existing duplicate selectors/properties and !important). Preserved per old-UI-first instruction; not waived or claimed passing. Copied package manifest needs rebuild tooling reconciliation.

## Runtime and handoff

Vite runs from interaction on http://127.0.0.1:1421/. Browser verified copied root/navigation and native-required notice; this is not functioning native chat. No native app, microphone/device QA, live inference, deployment or Git mutation performed by U1. Integration is connecting startup/basic chat and will coordinate native launch. Desktop/narrow native visual verification remains pending. L1 production reading contract has not been independently adopted.

## Final AI startup boundary

LogsOverlay and DevWindow retain existing docked/mobile/window frames but render a static “AI activity graph is not connected.” status. They no longer mount DevPanel, preventing old graph/run/retention/gate subscriptions and requests. Three focused tests pass for desktop/mobile reopen and separate-window mount with zero native calls. Integration notified that reserved openDevWindow wrapper still needed open_ai_window mapping. Other UI source is now frozen for coordinator QA.

Integration reported launching one native app from interaction and verifying saved conversation/translation, settings picker and coach through accessibility with no startup error banner. U1 did not launch a second app or independently claim device/live inference verification.

## Next round: read-only CSS audit

Prepared [finite CSS reconciliation audit](U1-css-audit.md): 71 checker failures classified, exact repeated-rule inventory, six important ownership resolutions and baseline/computed-style/native screenshot verification matrix. CSS/source remains frozen pending checkpoint. This is a plan, not implemented cleanup or completed visual verification.

## Current bounded handoffs

Dismissible inline errors are implemented and verified; see [dismissal evidence](U1-dismissible-errors.md). Latest Arabic source-size/composer-direction fix and verification are in [Arabic handoff](U1-arabic-reading.md). [Word speech](U1-word-speech-proposal.md) and [difficulty/contact controls](U1-partner-controls-proposal.md) remain proposals only; they do not authorize new inference or production contracts.

## Conversation controls and contacts implementation

[Bounded implementation handoff](U1-contact-settings.md): one five-stop difficulty slider using the generated native contract, autosaving contextual contact Profile, explicit contact chooser actions, obsolete steering removal and verification. Earlier control proposals are design history; generated-persona lifecycle remains deferred.

## Header and AI-access correction

User replaced the slider with a compact dropdown beside Native and requested a distinct controls surface. [Correction handoff](U1-header-access-correction.md) records measured full-pane/narrow geometry and AI-access unsaved-draft recovery. This supersedes slider presentation in earlier handoffs; native values/persistence are unchanged.
