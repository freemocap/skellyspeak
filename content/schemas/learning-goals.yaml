$schema: http://json-schema.org/draft-07/schema#
definitions:
  Construct:
    additionalProperties: false
    properties:
      band:
        type: string
      criterion:
        type: string
      id:
        type: string
      label:
        type: string
      lens:
        type: string
      nav:
        additionalProperties:
          type: string
        type: object
      opportunity:
        type: string
      requires:
        items:
          type: string
        type: array
      review:
        type: string
      sources:
        items:
          type: string
        type: array
      tokens:
        items:
          type: string
        type: array
      traits:
        items:
          type: string
        type: array
    required:
    - band
    - criterion
    - id
    - label
    - lens
    - nav
    - opportunity
    - requires
    - review
    - sources
    - tokens
    - traits
    type: object
items:
  $ref: '#/definitions/Construct'
title: Array_of_Construct
type: array
