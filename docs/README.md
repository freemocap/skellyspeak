# SkellySubs v2 [[R]] Project Docs

Source of truth for the design, specs, decisions, and working notes. The root
`DESIGN.md` is the high-level overview; this folder holds the expanding detail.

## Structure

- `way-of-working.md` [[R]] spec-driven + TDD process, checkpoint cadence
- `testing.md` [[R]] testing strategy (unit / integration / e2e)
- `specs/` [[R]] feature specs (acceptance criteria + test plans)
- `decisions/` [[R]] architecture decision records (ADRs)
- `checkpoints/` [[R]] compile + test checkpoint log

## Status

MVP: chat-first cross-language conversation partner (push-to-talk [[R]] word-aligned
transcript [[R]] LLM tutor reply). Video subtitling is a later mode.
