The SkellySpeak mark, 512×512 PNG with transparency (source: ui/public/skellyspeak-logo.png; also the app icon). Full colour only: teal skull (close to `chat-stripe-0`), red sparkles and greeting text (close to `red-500`), black outline. Place it on `bg`, `chrome` or `sheet`; it reads on both themes because of its black outline and light bubble. Minimum size 28px (top bar). Don't recolour, crop or redraw it.
