# Copy to local.env with owner-only permissions (chmod 600 local.env).
# Fill in provider keys locally. Do not put real credentials in this sample.
# One KEY=value per line; optional quotes, no shell expansion.
OPENROUTER_API_KEY=
GROQ_API_KEY=
