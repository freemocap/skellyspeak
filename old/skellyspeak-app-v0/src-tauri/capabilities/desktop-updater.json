{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-updater",
  "description": "In-app update check and install. Desktop only — Android and iOS get updates from the store or a sideloaded package, and the updater plugin is not compiled for them.",
  "platforms": ["macOS", "windows", "linux"],
  "windows": ["main"],
  "permissions": ["updater:default", "process:allow-restart"]
}
