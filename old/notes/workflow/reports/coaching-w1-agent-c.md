# Wave 1 report · Agent C · Experience & Game

You are **Agent C**, the Experience & Game agent for SkellySpeak's coaching work. You own what the learner *sees and feels*: chat surfaces, the coach card, the start panel, the profile, and reward presentation. Two other agents work in parallel: A (Language & Config) and B (Coach & Learner Core, Rust). You never talk to them directly; the integration agent coordinates through reports like this one.

## Read first

1. `skellyspeak-docs/docs/coaching-plan.md`: §1, §8 (edit & retry), §9 (UI surfaces), §11 (game layer; context for later waves).
2. `skellyspeak-docs/docs/coaching-work-plan.md`: your ownership row, the cycle, the house rules, the hand-back template.
3. `skellyspeak-docs/docs/coaching-contracts.md` → **Wave 1 · B → C**. You consume `ReviseTurn` and the snapshot fields.
4. `AGENTS.md`, `ui-guidelines.md`, `UI-SURFACES.md`.

## Your files

**Edit only:**

- `src/features/**`, `src/state/**`, `src/styles/**`, `src/ui/**`, `src/platform/audio/**`
- frontend tests

`src/contracts.ts` is generated by B; never edit it. A owns language helpers except for the integration-assigned `src/domain/language/conversation-view.ts` and its tests, which you own for this wave. You also own `src/domain/skills/**` and `src/types.ts` for the bounded revision/type/reward work. B alone edits `src/assets/skill-catalogs/catalog.json`. Read `workflow/reports/coaching-w1-integration.md` and the contracts' Revision integration requirements.

## Background (verified by integration on 2026-09-12; re-check line numbers)

Edit & try again is fully built in the UI but unreachable:

- `GuidedPage.tsx` (~line 541) passes `onEditUser={undefined}` to `TurnView`.
- `requestTurn` (~line 276) rejects `replacesMessageId` with "This action is not connected yet."
- `editingTurnId` is only ever set to `null`, so `revision` in `inputEvidence` is always false.

The buttons exist in `MessageFeedback.tsx` ("Edit & try again", gated on `onEdit`), `PartnerReaction.tsx` (`disabled={!onEdit}`) and `TurnView.tsx` (`onEditUser`). The edit banner, `EditFeedback` and `cancelEdit` exist in `GuidedPage`.

`TurnView.test.tsx` supplies `onEditUser = vi.fn()` and passes, although the app never passes the handler. It's a mock-only test that hides the defect.

## Tasks

1. **Scaffold first (no dependency).**
   - Build rendering for revised turns against a fixture snapshot shaped like the contract (`replacesTurnId`, `replacedBy`): the replaced partner reply shows collapsed as "Earlier version" under the new turn, and expands on click.
   - Write the GuidedPage integration test skeleton.
2. **Wire it (after B lands `ReviseTurn` and regenerates contracts).**
   - Connect `onEditUser` to begin an edit (set `editingTurnId`, prefill the composer, banner).
   - Send through the new `reviseTurn` action instead of `requestTurn`, and delete the "not connected yet" rejection.
   - Editing an earlier turn asks for confirmation, naming how many later turns will be removed (use an in-app dialog consistent with `ui-guidelines.md`, not `window.confirm`).
   - While a turn is pending, the edit control is disabled, and a typed error from Rust is shown if the action races.
3. **Tests.**
   - Replace the mock-only assertion in `TurnView.test.tsx` with a GuidedPage-level integration test: render with a real handler, click Edit & try again, edit, send, and assert that the action payload and the resulting snapshot rendering are correct.
   - Add a test that the edit control is actually enabled when a turn is editable, so the defect can't recur silently.
4. **Rewards in wave 1.** Leave `skill-rewards.ts` logic as is unless B's hand-back changes the credit rule; the support-weighted credit arrives in wave 3. If B changes evidence-filter semantics (catalog version), adapt `message-evidence.ts` / `skill-rewards.ts` to match, and report it.

5. **Integration requirements.** Carry B's durable message/turn identity through the presentation adapter; never send numeric sequence as `turnId`. Group by identity, including page boundaries and repeated revisions. Present the exact native removal scope and retain the draft when a stale confirmation is rejected. Consume the generated Outcome in `skills.ts`. Once B verifies existing 2-XP assisted revision credit, remove the blanket replacement exclusion and test positive net credit, deduplication, exclusions and refresh/reopen without replay. Do not infer semantic correctness from a passing fixture.

## Paid inference

None for source and fixture verification. A real edit/regenerate check creates provider work; coordinate a single bounded native QA run through integration, state model and maximum request count before dispatch, and report actual calls. Do not claim live completion from mocks or a browser fixture.

## Done means

- The gate passes.
- Edit & try again passes the frontend/native fixture path. After B's schema work is accepted, coordinate the native run (development data resets are already authorized): one latest-turn edit and one earlier-turn edit, with actual provider calls bounded as above. Report source handoff and native QA separately if the runtime gate is not yet ready.
- The mock-only test is gone.
- Hand-back in the template format.
