# U1 — dismissible errors and composer regression check

User explicitly requested dismissal for the in-place error bubbles. Added an accessible close button inside each summary, preserving disclosure and error location. Dismissal is local presentation state; no requests, retries or durable error mutation. Semantic error keys keep an acknowledged notice dismissed through parent rerenders (including recreated React children). A changed error or newly mounted occurrence appears again. Keys include operation/state/error for gloss and coach turn/error for coaching. Identical errors without an intervening remount or changed key are treated as the same notice.

Source: ErrorDetails.tsx, error-key props in GuidedPage, GlossAssistance, ComposerHelp and CoachAnalysisPanel; scoped close-button CSS. New ErrorDetails.test.tsx covers collapsed keyboard dismissal, expanded pointer dismissal, stable dismissal across rerenders and reappearance for a new key.

Text regression investigation: extracted ComposerInput retains controlled value/onChange, native form submit, .field under existing composer ref and availability gate. No source regression reproduced. Added two userEvent full-page tests for typed accented input, Enter/Send admission exactly once, authentication rejection restoring draft and re-enabling Send. Integration independently observed backend receipt delivery with custom-route authentication failure; native error wording/auth repair remains its ownership.

Verification: build and style check passed; 76 frontend files / 358 tests passed. Actual browser ComposerInput fixture submitted exact text once via Enter and once via Send, then cleared input. Dismissal fixture at 390×844: pointer dismissal removed notice, new error appeared, expanded notice dismissed by Enter; no horizontal overflow. Screenshot inspected; viewport reset. Fixtures at http://127.0.0.1:1423/input.html and /dismiss.html contain no native calls/inference. No native Send or model calls, no Git writes. Native QA remains integration-owned.
